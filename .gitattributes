# Use LF for all text files (cross-platform friendly)
* text=auto eol=lf

# Use CRLF only for YAML if needed (you can remove this if LF is fine)
*.yml text eol=crlf
*.yaml text eol=crlf

# Treat binary files correctly
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary

# C#-specific settings
*.cs text
*.csproj text
*.sln text
*.config text
*.resx text
